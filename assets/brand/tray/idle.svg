<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64" role="img" aria-label="revu tray icon — idle">
  <title>revu tray — idle</title>
  <!-- Glyph mono r-monogram (REV-48) sobre fundo transparente. Sem badge: idle. -->
  <text x="32" y="46"
        text-anchor="middle"
        font-family="Inter, 'Inter Variable', system-ui, sans-serif"
        font-weight="600"
        font-size="44"
        fill="#FFFFFF"
        letter-spacing="-2">r</text>
</svg>
